const start = {
  renderer: 'mis-html',
  html: 'Hello World!'
};

const demoSchema = {
  renderer: 'mis-html',
  html: '<div><h3>const json = {renderer: "html", html: "Hello World!"};</h3><h3>&lt;mis-schema \:initSchema=json \/&gt;</h3></div>'
};

export { start, demoSchema };
